type: object
additionalProperties: false
required:
  - id
  - slug
  - revision
  - federationVersion
  - createdAt
  - updatedAt
properties:
  id:
    type: string
    format: uuid
  slug:
    type: string
    minLength: 1
  revision:
    type: string
    pattern: "^[1-9][0-9]*$"
  federationVersion:
    type: string
  createdAt:
    type: string
    format: date-time
  updatedAt:
    type: string
    format: date-time
