type: object
additionalProperties: false
required:
  - slug
  - federationVersion
properties:
  slug:
    type: string
    minLength: 1
  federationVersion:
    type: string
    pattern: "^v[0-9]+\\.[0-9]+$"
